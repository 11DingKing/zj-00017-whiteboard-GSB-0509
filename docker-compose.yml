version: '3.8'

services:
  whiteboard:
    build: .
    container_name: whiteboard-dev
    ports:
      - "16017:5173"
    volumes:
      - ./src:/app/src
    environment:
      - NODE_ENV=development
    command: npm run dev
